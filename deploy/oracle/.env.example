# Set this to the Oracle instance public IP at first. A domain can replace it later.
PUBLIC_HOST=203.0.113.10

# Secrets stay only on the server. Do not commit the real values.
DEEPSEEK_API_KEY=sk-replace-me
RESOURCE_EXTRACTOR_LLM_ENABLED=true
RESOURCE_EXTRACTOR_BASE_URL=https://api.deepseek.com
RESOURCE_EXTRACTOR_MODEL=deepseek-chat
WEWE_RSS_AUTH_CODE=replace-with-a-long-random-string
